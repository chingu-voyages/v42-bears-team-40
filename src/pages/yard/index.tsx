import Layout from '../../components/Layout';

type Props = {};

const Yard = (props: Props) => {
  return (
    <Layout>
      <div>Seller Main Page</div>
    </Layout>
  );
};

export default Yard;
